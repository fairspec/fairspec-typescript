pre-commit:
  jobs:
    - name: scan:secret:staged
      run: pnpm scan:secret:staged
    - name: format:staged
      run: pnpm format {staged_files}
      stage_fixed: true

pre-push:
  jobs:
    - name: test
      run: pnpm test
